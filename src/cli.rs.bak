use std::ffi::OsString;

use clap::Parser;

#[derive(Parser, Debug)]
#[command(author, version, about, long_about = None)]
pub struct Cli {
    /// 监视的目录或文件，可以是多个，若无则为当前命令运行目录
    Path: Option<Vec<OsString>>,

    #[arg(short)]
    access: bool,

    #[arg(long)]
    Attrib: bool,
}
